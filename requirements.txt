# 向量库（嵌入式模式，无需独立服务）
chromadb==0.4.24

# Embedding 模型（会自动拉取 torch CPU 版本）
sentence-transformers>=2.7.0

# Web UI
streamlit>=1.35.0

# 文档解析
pymupdf>=1.24.0
python-docx>=1.1.0

# HTTP 请求（Ollama / API 调用）
requests>=2.31.0

# 内存监控
psutil>=5.9.0

# 测试框架
pytest>=7.0.0
