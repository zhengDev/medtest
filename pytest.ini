[pytest]
testpaths = tests
python_files = test_*.py
python_classes = Test*
python_functions = test_*
addopts = -v --tb=short
markers =
    unit: 单元测试（快速，无外部依赖）
    integration: 集成测试（需要 Embedding 模型，较慢）
    slow: 慢速测试（需要 LLM 服务）
